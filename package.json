{
  "name": "attendance-stack",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel --filter=web --filter=mobile",
    "dev:web": "pnpm --filter web dev",
    "dev:expo": "pnpm --filter expo dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
